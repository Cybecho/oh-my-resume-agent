# EN Resume DOCX Template

Place your DOCX template file at:

- `templates/docx/en_resume_template.docx`

Step 8 publish uses `docxtpl` and `config/en_resume_publish.json` to render this template.
If the template is missing, `docx` target is marked as failed and step 8 will be `blocked`.
