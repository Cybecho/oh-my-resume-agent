#!/usr/bin/env node
import { main } from './omr';

main(process.argv.slice(2), { alias: 'resume' }).catch((error) => {
  console.error(error instanceof Error ? error.message : String(error));
  process.exitCode = 1;
});
