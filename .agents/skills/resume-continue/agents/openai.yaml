interface:
  display_name: "이어서 진행"
  short_description: "중단 상태를 복원해 다음 단계부터 작업을 이어 실행"
  default_prompt: "Use $skill-name to resume work from the latest incomplete step."
  policy:
    allow_implicit_invocation: true
