interface:
  display_name: "EN Company Research"
  short_description: "Research company context and ATS keyword bank"
  default_prompt: "Use $skill-name to run EN company research with fallback support."
  policy:
    allow_implicit_invocation: true
