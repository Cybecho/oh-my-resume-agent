# 매칭 — Step 4-5 단독 재실행

경험 인덱싱 + 매칭 단계를 단독으로 재실행한다.

## 사전 조건

1. `output/` 폴더에서 대상 회사 폴더를 확인한다
   - 여러 개면 사용자에게 선택 요청
2. 해당 폴더의 `state.json`을 읽는다
3. `02_JD분석.md`와 `03_의도분석.md`가 존재하는지 확인

## 실행

1. `agents/기억선생.md`를 Read로 읽는다
2. 02_JD분석.md, 03_의도분석.md, 01_기업조사.md 읽기
3. 로컬 `workspace/experience_cards/`에서 `doc_type: experience_card` 기반으로 1차 후보군 구성
4. `workspace/claims/claim_registry.yaml`에서 approved claim 기반 수치 검증
5. `workspace/feedback/log.json`에서 과거 제출 피드백을 읽기
6. 기존 개인용 seed에서만 `data/experience_cards/`, `data/feedback/log.json`, `data/experiences/_index.md`를 legacy 보완 소스로 읽기
7. 1차 인덱싱 → `04_경험풀.md` 덮어쓰기 저장
8. 2차 인덱싱 → `05_경험매칭.md` 덮어쓰기 저장
9. `state.json` 업데이트: step 4,5 → completed

## 완료 후

- 매칭 결과 요약 (문항별 1순위 경험 목록)
- "이후 단계를 재실행하려면 `/기획`을 사용하세요."
